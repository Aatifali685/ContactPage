import React from "react";
import styles from "./Contactheader.module.css";
const Contactheader = () => {
  console.log(styles);
  return (
    <div className={`${styles.content}`}>
      <h1>CONTACT US</h1>
      <p>
        Let's coonect with the clients and costumers by just a click. Let's make
        the community closer and build up sound environment.Let's coonect with
        the clients and costumers by just a click. Let's make the community
        closer and build up sound environment.Let's coonect with the clients and
        costumers by just a click.
      </p>
    </div>
  );
};

export default Contactheader;
